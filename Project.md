# UNet Assignment Execution Brief

## 文档用途

这份文档不是写给人类同学做作业汇报的，而是写给后续执行项目的 AI 代理。

目标有两个：

- 让 AI 在最短上下文内理解当前仓库真实状态，而不是按作业题面做错误假设。
- 把整个 assignment 拆成 3 个知识域明确、顺序明确、验收明确的子任务，便于逐步完成。

这份文档强调：

- 真实仓库上下文
- 任务边界
- 风险提示
- 每个 task 的知识要求、算法建议、最严格测试标准

这份文档**不提供具体实现步骤**，也**不替代代码阅读**。

## 当前项目的真实目标

基于当前仓库中的超大幅面遥感影像与建筑标签，完成一个二分类建筑提取流程：

1. 从大图裁剪出训练/验证样本集。
2. 使用当前工程中的 UNet 基线完成训练与验证。
3. 对测试大图执行滑窗预测、拼接恢复、IoU 评价与误差可视化。
4. 在基线闭环正确后，再实现并比较以下改进：
   - 带重叠率的训练裁剪扩增
   - 带重叠率的测试滑窗融合
   - EfficientNet backbone 替换
   - CBAM 注意力嵌入

## 仓库现状

### 顶层结构

- `/home/hunry/Desktop/UNet/`
  - `任务二：UNet语义分割实现.docx`
  - `Project.md`
  - `code/`

### `code/` 当前文件

- `step1_crop_dataset.py`
  - 负责将 `img_trainval.png` 与 `label_trainval.png` 裁剪并划分为训练/验证集。
  - 当前仍然只是目录创建骨架，核心裁剪逻辑尚未补完。
- `dataset.py`
  - 已存在 `MyDataset`，负责训练/验证集读取、二值标签处理、归一化、基础增强。
- `step2_train.py`
  - 已存在完整训练主循环、验证逻辑、checkpoint 保存逻辑。
- `unet_model.py`
  - 已存在基线模型 `Res34UNet_light`，基于 `resnet34` encoder。
- `eval.py`
  - 已存在基于混淆矩阵的 IoU 评价实现。
- `step3_predict.py`
  - 当前仍然只是测试推理任务说明骨架，尚未实现真正的滑窗推理、拼接、评价、可视化。
- `img_trainval.png` / `label_trainval.png`
  - 训练验证源数据。
- `img_test.png` / `label_test.png`
  - 测试数据与评价标签。

## 关键现实约束

后续 AI 必须以这些约束为准：

- 这是一个**二分类语义分割**任务：背景 vs 建筑。
- 数据格式是普通 PNG，不要假设存在地理坐标、投影、矢量边界或遥感元数据。
- 当前工程脚本大量使用**相对路径**，默认工作目录大概率应是 `code/`，而不是仓库根目录。
  - 例如 `step2_train.py` 内部使用 `./dataset/train/image/`。
  - 因此后续实现时，必须明确“数据集目录相对于哪个 cwd 存在”。
- 当前代码依赖并不完全轻量，至少涉及：
  - `torch`
  - `torchvision`
  - `numpy`
  - `PIL`
  - `imgaug`
  - `skimage`
- 当前环境不保证这些依赖已全部可用。不要把“代码文件存在”误判为“项目已可直接运行”。

## 作业文本与真实工程的差异

后续实现必须以真实文件与脚本行为为准，不要机械按作业描述中的尺寸写死：

- 作业文本中提到的尺寸与当前实际文件尺寸不完全一致。
- 现有代码文件也不是完整答案，而是“部分已实现、部分留空”的半成品工程。
- 因此：
  - 裁剪窗口覆盖策略
  - 测试图恢复尺寸
  - 滑窗边界处理
  - 评价输入尺寸对齐
  
  都必须基于真实读入图像的尺寸动态处理，而不是硬编码。

## 已实现能力与未实现能力

### 已基本具备

- 基线模型定义
- 训练主循环
- 验证集 IoU 评价逻辑
- 基础数据增强接口
- 训练集/验证集的 dataset 类接口

### 仍是核心缺口

- 大图裁剪与 70/30 划分逻辑
- 样本命名与可追溯性设计
- 测试集滑窗推理与拼接
- 概率融合或重叠融合机制
- `predict.png` 输出
- `visualization.png` 输出
- 基线与改进实验的统一对比框架

## AI 执行总原则

- 先保证**数据正确**，再保证**训练正确**，最后保证**推理与改进正确**。
- 任何模型结构优化都不能绕过基线闭环。
- 优先修复接口、路径、shape、dtype、标签语义、阈值逻辑这类基础问题。
- 只要结果异常，首先排查数据与评价，不要第一时间怀疑 backbone 不够强。
- 所有对比实验都必须保证控制变量，否则结论无效。
- 后续若新增模块，尽量做成可开关配置，方便消融。

---

## Task 1：训练数据构建与数据闭环确认

### 任务定位

这是一个**数据工程 + 样本组织**问题。

这个 task 的目标不是“多裁几个块”，而是建立一个可靠的数据入口，使后续训练和验证拥有：

- 正确的图像/标签对齐
- 完整的边界覆盖
- 可复现的样本划分
- 可追踪的文件命名

如果这个 task 做错，后面的训练和改进几乎都会失去意义。

### 该 task 必须完成的具体任务

- 读取训练大图 `img_trainval.png` 和对应标签 `label_trainval.png`。
- 检查图像与标签的空间尺寸是否一致；若不一致，直接视为阻塞问题。
- 设计并实现 `512 x 512` 的无重叠裁剪逻辑。
- 确保裁剪逻辑完整覆盖原图，包括最右侧和最下侧边界区域。
- 为每一个图像块生成对应的标签块，二者必须使用完全相同的窗口坐标。
- 设计可追溯的命名规则，使任意一个 patch 都能回溯其来源位置。
- 构建以下目录并输出样本：
  - `dataset/train/image/`
  - `dataset/train/label/`
  - `dataset/val/image/`
  - `dataset/val/label/`
- 按 `70% / 30%` 划分训练集与验证集，并保证划分可复现。
- 统计并输出至少以下信息：
  - 总 patch 数量
  - train patch 数量
  - val patch 数量
  - 实际使用的窗口参数
- 为后续“带重叠率训练裁剪扩增”预留参数化扩展空间，但不要在基线阶段提前污染验证集。

### 所需知识

- 大图滑窗裁剪原理
- 图像与标签同步裁剪
- 图像块命名与索引设计
- 数据划分中的随机种子控制
- 遥感任务里的空间泄漏与近重复样本问题
- 二值分割标签的一致性处理

### 算法建议

- 基线版本应先采用：
  - `tile_size = 512`
  - `stride = 512`
- 不允许因为尺寸不能整除就直接丢弃边界区域。必须保证原图每个像素都被至少一个裁剪块覆盖。
- 更稳妥的边界策略是：
  - 常规区域按固定 stride 滑动
  - 最后一行和最后一列窗口采用“贴边”坐标
- 图像块和标签块必须严格共享同一组坐标。
- 文件命名建议编码坐标或 tile id，使每个样本可追溯回原图位置。
- `70% / 30%` 划分必须可复现，固定随机种子。
- 如果后续需要做带重叠率的训练集扩增，建议只对训练集做，不要把高重合块混进验证集。
- 更严格地说，验证集应尽量保持“空间上更独立”，避免因为相邻块高度相似而高估泛化性能。

### 最严格测试要求

- 必须验证所有输出图像块与标签块尺寸恒为 `512 x 512`。
- 必须验证裁剪后图像与标签仍然像素级对齐，不能出现哪怕 1 个像素的系统性偏移。
- 必须验证原图右边界和下边界没有漏裁。
- 必须验证训练集与验证集样本一一对应、无重名、无错配。
- 必须验证随机划分是可复现的，同一随机种子下多次执行结果一致。
- 必须进行人工可视化抽检，至少确认若干建筑边界块没有裁剪错位。
- 如果引入重叠扩增，必须验证验证集没有因空间重叠而受到污染。

### 该 task 完成后的最低交付状态

- 可以在当前工程中稳定生成训练/验证数据目录。
- 训练脚本可以直接消费这些目录，而无需手工重命名或搬运文件。
- 人工抽样检查时，图像块与标签块在建筑边缘处对齐合理。
- 数据划分方案可以被记录和复现。

---

## Task 2：基线训练、验证与可复现实验基线

### 任务定位

这是一个**PyTorch 训练管线 + 分割评价闭环**问题。

目标是基于现有 `dataset.py`、`step2_train.py`、`unet_model.py`、`eval.py`，建立一个真正可运行、可保存、可复现的基线训练系统，并明确它的评价语义。

### 该 task 必须完成的具体任务

- 确认 `dataset.py`、`step2_train.py`、`unet_model.py`、`eval.py` 在当前工程内接口可连通。
- 确认训练脚本实际读取的是 Task 1 输出的数据目录，而不是错误路径。
- 核查并修正训练流程中的关键接口问题，包括但不限于：
  - 数据路径
  - checkpoint 路径
  - 模型加载逻辑
  - tensor shape 与 dtype
- 保持当前 `Res34UNet_light` 作为唯一基线模型，不在这一阶段引入结构改动。
- 跑通一次完整训练流程：
  - 训练集读取成功
  - 验证集读取成功
  - loss 能正常下降
  - 验证能正常输出 IoU
- 保存验证集最优模型权重。
- 训练结束后重新加载最佳权重，并再次验证其性能。
- 明确并固定基线的训练配置语义：
  - 输入通道
  - 标签二值化规则
  - 归一化参数
  - 推理阈值
  - IoU 定义
- 形成一个后续所有改进实验都必须对齐的“基线标准”。

### 所需知识

- `Dataset` / `DataLoader` 机制
- 图像张量和 mask 张量的 shape 约束
- `BCEWithLogitsLoss` 的输入输出语义
- 模型 `train()` / `eval()` 行为差异
- checkpoint 保存与恢复
- 二分类 IoU 的计算逻辑
- 预训练 backbone 的迁移学习使用方式

### 结合当前代码的关键观察

- `MyDataset` 已实现基础增强，训练时会做：
  - `Rot90`
  - 垂直翻转
  - 水平翻转
- `MyDataset` 中标签通过 `>0` 转为二值 mask，因此后续所有评价与可视化都必须遵守同一语义。
- `step2_train.py` 使用固定 `mean/std` 做归一化，因此测试阶段必须沿用同一套参数。
- `Res34UNet_light` 在 `self.training == True` 时返回 `(loss, out)`，推理时只返回 `out`。后续推理脚本必须遵守这个接口。
- `eval.py` 默认在单通道输出时执行 `sigmoid -> threshold(0.5)`。测试脚本评价时必须保持一致，否则数值无法可比。
- `step2_train.py` 内部对 checkpoint 路径存在相对路径使用不一致的风险，后续执行时必须重点检查 cwd 与文件存在性。

### 算法建议

- 第一阶段不要改损失函数、不要改 backbone、不要加注意力模块，先让当前 `Res34UNet_light` 成为唯一可信基线。
- 数据读取逻辑必须确保：
  - 输入图像为 3 通道
  - 标签为单通道二值图
  - 张量类型与设备一致
  - 训练/验证/测试使用同一套归一化参数
- 若训练初期出现异常，应优先排查：
  - 数据目录路径
  - 训练与验证集是否非空
  - 标签是否真的只有背景/建筑两类
  - loss 输入 shape 是否与标签对齐
  - checkpoint 是否真正写到预期目录
- 基线评价应围绕验证集 IoU 最优模型展开，不要用最后一个 epoch 作为正式基线。
- 若后续要引入 Dice、加权 BCE、Focal 等改进，必须在基线结果稳定后再进行。

### 最严格测试要求

- 必须通过“小样本过拟合测试”：少量样本上应能快速逼近极高训练精度，否则说明数据或 loss 存在问题。
- 必须验证训练阶段与验证阶段没有 shape、dtype、device 错误。
- 必须验证 checkpoint 保存后可正确加载，并在验证集上得到一致结果。
- 必须验证阈值化逻辑统一，不能训练看 logits、验证看 sigmoid、测试又换成别的阈值方案。
- 必须验证评价函数中的前景/背景编码语义与数据集中标签语义一致。
- 必须确认当前增强不会破坏 image-mask 同步关系。

### 该 task 完成后的最低交付状态

- 可稳定输出一个基线最佳模型权重文件。
- 验证集 IoU 可以被重复计算，且数值逻辑前后一致。
- 基线训练流程具备可复现性和可解释性，能作为后续所有比较实验的参照。
- 出现异常时，可以明确定位问题属于数据、模型、loss、评价还是路径层面。

---

## Task 3：测试大图推理、误差分析与改进方案对比

### 任务定位

这是一个**大图推理工程 + 实验设计**问题。

目标是基于已训练好的基线模型，完成测试图像的滑窗预测、拼接恢复、IoU 评价、误差可视化，并为改进实验提供统一对比框架。

### 该 task 必须完成的具体任务

- 加载 Task 2 得到的基线最佳模型权重。
- 明确测试阶段所需的全部预处理语义，并与训练阶段保持一致。
- 针对 `img_test.png` 实现大图滑窗推理逻辑。
- 先完成无重叠滑窗推理基线版本，确保能够生成完整预测图。
- 将所有 patch 预测结果拼接恢复成整幅测试图大小。
- 输出 `predict.png`，尺寸必须与测试标签严格一致。
- 基于 `label_test.png` 计算测试集 IoU，并明确该 IoU 与验证集 IoU 的定义是否一致。
- 生成误差可视化图：
  - 背景误判成建筑为红色
  - 建筑漏检为绿色
- 在无重叠基线正确后，扩展并实现：
  - 带重叠滑窗推理
  - 重叠区域概率融合或加权融合
- 在测试推理闭环稳定后，再组织改进实验：
  - 带重叠训练裁剪
  - 带重叠测试融合
  - EfficientNet backbone
  - CBAM 嵌入方案
- 为每个改进实验输出统一可比较的记录，而不是只保留“最好结果”。

### 所需知识

- 大图滑窗推理
- 分块预测结果拼接
- 概率图融合与重叠平均
- 阈值化时机选择
- 错分类型可视化
- 控制变量实验设计
- backbone 与注意力模块的公平对比方法

### 结合当前代码的关键观察

- `step3_predict.py` 目前仍未实现，后续 AI 不能误判为“项目只差调参”。
- 当前训练与验证阶段的归一化和阈值逻辑已经隐式确定，因此测试阶段不能自创另一套预处理。
- 当前评价代码是 patch 级 DataLoader 风格实现；测试阶段做全图预测时，若直接复用该函数，通常需要额外包装或重写全图评价逻辑。

### 算法建议

- 对测试大图的预测，优先生成**概率图**或 **logits 累积图**，最后再全图统一阈值化，不要先对每个 patch 独立二值化再拼接。
- 基线先做无重叠滑窗推理，确认闭环正确后再做带重叠融合。
- 对带重叠预测，更推荐：
  - 每块输出概率
  - 维护一个累加图
  - 再维护一个计数图或权重图
  - 最终按逐像素平均融合
- 若要进一步降低网格缝，可考虑非均匀权重融合，例如中心高权重、边缘低权重。
- 可视化必须至少区分：
  - False Positive：背景被误判成建筑，标红
  - False Negative：建筑被漏判成背景，标绿
- 改进实验必须按单变量方式组织，推荐顺序：
  1. 基线：无重叠训练 + 无重叠推理
  2. 带重叠训练裁剪
  3. 带重叠测试融合
  4. EfficientNet backbone
  5. CBAM 插入方案对比
- CBAM 的插入位置不应拍脑袋决定。应把 encoder、decoder、skip connection 视为待比较设计点，而不是默认某一处一定最优。

### 最严格测试要求

- 必须验证最终全图预测尺寸与测试标签尺寸完全一致。
- 必须验证滑窗遍历顺序不会改变最终拼接结果。
- 必须在可控小样例上验证：滑窗推理与直接整图推理在无重叠条件下结果应一致或非常接近。
- 必须验证 `predict.png`、IoU 数值、`visualization.png` 使用的是同一份预测结果。
- 必须验证重叠融合没有出现覆盖错误、计数错误或边缘未归一化错误。
- 必须对每个改进实验输出统一对比记录，至少包含：
  - 方法名
  - 相对基线的唯一关键改动
  - IoU
  - 主要现象
  - 额外代价（显存、时间、复杂度）

### 该 task 完成后的最低交付状态

- 可以对整幅测试图完成自动化预测与评价。
- 可以输出完整预测图和误差可视化图。
- 至少存在一个可比较的基线测试结果。
- 改进实验具备统一记录格式，能够支持后续分析与作业总结。

---

## 三个 Task 之间的依赖关系

- Task 1 是 Task 2 的前置条件。
- Task 2 是 Task 3 的前置条件。
- 只有在 Task 3 的基线推理闭环完成后，改进实验才有意义。

如果前一个 task 的最严格测试没有通过，不应推进下一个 task。

## 推荐的完成定义

只有同时满足以下条件，才应视为项目真正完成：

- 数据集成功构建，且训练/验证划分可靠。
- 基线训练脚本可以稳定运行并保存最佳权重。
- 测试大图可以完成滑窗预测并输出完整预测图。
- 可输出 IoU 评价结果和误差可视化图。
- 改进实验至少形成一组可解释、可比较的结果。
- 所有结果的生成逻辑与评价逻辑前后一致，没有“训练一套语义、测试另一套语义”的问题。

## 一句话执行摘要

把这个 assignment 当成三个递进系统来做：先构建可信数据入口，再构建可信训练基线，最后构建可信大图推理与改进实验框架；任何绕过前置正确性的优化，都是无效进展。
